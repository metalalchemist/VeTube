# VeTube — OpenSpec Configuration
# Auto-generated by sdd-init on 2026-06-06

context:
  project: VeTube
  description: wxPython GUI application for watching TikTok/Kick live streams with
    TTS, translation, and accessibility support.
  stack:
    language: Python >=3.13
    build: uv
    gui: wxPython >=4.2.5
    media: yt-dlp, python-vlc
    streaming: TikTokLive, kick-py, pytchat, chat-downloader
    audio: sounddevice, sound-lib, accessible-output2, espeak-phonemizer
    ml: onnxruntime, numpy==1.26.4
    platform: Windows (x86/x64)
  persistence_mode: openspec

strict_tdd: false
strict_tdd_reason: "No test runner detected. Project has no pytest/unittest config,
  no test directory, no test dependencies, and no CI test step. The only test-like
  file is test_tiktok_url.py, a standalone script. Strict TDD cannot be enforced
  without a test framework."

testing:
  runner:
    available: false
    command: null
    framework: null
    reason: "No pytest.ini, tox.ini, setup.cfg, or test dependencies found in pyproject.toml.
      CI workflows only build PyInstaller binaries — no test step."
  layers:
    unit:
      available: false
      tool: null
    integration:
      available: false
      tool: null
    e2e:
      available: false
      tool: null
  coverage:
    available: false
    command: null
  quality:
    linter:
      available: false
      command: null
    type_checker:
      available: false
      command: null
    formatter:
      available: false
      command: null

phases:
  propose:
    required: true
  spec:
    required: true
  design:
    required: true
  tasks:
    required: true
  apply:
    required: true
  verify:
    required: true
  archive:
    required: true
